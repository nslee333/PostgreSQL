#!/bin/bash


if [[ -z $1 ]]
then
    echo "Please provide an element as an argument."

# If $1 == atomic number
    # query database for correct entry
    # format string to echo what it wants.
    # blah blah what it is etc  

# if $1 == symbol of element
    # blah blah

# IF $1 == name of element
    # blah blah

# else 
    # echo "I could not find that element in the database."





fi
